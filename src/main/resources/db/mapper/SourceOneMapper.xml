<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tkurek.wat.Etl.mapper.SourceOneMapper">

    <select id="testSelect" resultType="java.lang.String" parameterType="long">
        select p.name
        from product as p
        where p.idProduct = #{id}
    </select>

</mapper>